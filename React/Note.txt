                                    Part-1
React 
    JS Library for creating UI 

    rect.dev

    component -> like a function we can reused 
              -> we can render(display) a component where as in function we call(invoke)
        
JSX:(not real js)
    JavaScript Extention Syntax

    It lets us write HTML directly inside JavaScript

    JSX --> transpile(babel) --> JS

Set Up Local Environment:

    Create-React-App v/s Vite

    npm create vite@latest

    npm run dev // to start the srver

Rewrite App:
    import "./App.css";

    function App(){
        return <h1>Hello,World</h1>
    }

    export default App;

Our First Component:
    Component is a reusable & independent peace of code.

    Crate a component
        function Title(){
            return (
                <h1>hellow,worl!</h1>
            );
        }

    Rendering a Component
    <Title></Title> OR <Title/>

Import - Export:
    Import 
        import Title from "./Title.jsx";

    Default Export:
        export Default Title
    
    Named Export:
        export{title}
        import {Title} from "./Title.jsx"

In summary, named export are useful when you want to export multiple values and import them  with their specific names, while default export are handy for exporting a single value and giving it a custom name when importing.The choice between the two depends on the structure and requirements of your codespaces.

Writing Markup in JSx:
    1.Return a single root element
    2.Close all the tag
    3.camelCase most of the things

Rect Fragment:
    This empty tag(<></>) is called a Fragment. Fragments let you group things without leaving any trace in the browser HTML tree.

JavaScript in JSX with Curly Braces:
    function Description() {
        let description = "body";
        return <h3>I am the {description}</h3>;
        }
Structring Component:    
    import Product from "./Product.jsx";
        function ProductList() {
        return (
            <>
            <Product />
            <Product />
            <Product />
            </>
        );
        }
        export default ProductList;

Style Component:

    import "./Product.css";

    function Product() {
    return (
        <div className="Product">
        <h3>This is a Tiltel of the product </h3>
        <h5>We can know the product description detial </h5>
        </div>
    );
    }
    export default Product;

    .Product {
        border: 1px solid green;
        border-radius: 16px;
        margin-bottom: 15px;
        padding: 0 10px 0 10px;
        }

                                    Part-2
React Props:
    Props are the information that you pass to a JSX tag. For example, className, src, alt, width, and height are some of the props you can pass to an <img>

    import Product from "./Product.jsx";
    function ProductList() {
    return (
        <>
        <Product title="Mobile" price={30000} />
        <Product title="Laptop" price="130000" />
        <Product title="Pen" />
        </>
    );
    }
    export default ProductList;

    import "./Product.css";
    function Product({ title, price = 1 }) {
    return (
        <div className="Product">
        <h3> {title}</h3>
        <h5> Price:{price}</h5>
        </div>
    );
    }
    export default Product;

Passing Arrays To Props:
    features = {["fast","relaible"]}
    features = {{a:"fast",b:"relaible"}}

Rendering Arrays:
        <p>
            {features.map((el) => (
            <li> {el} </li>
            ))}
       </p>
Conditonals:
    Adding element on the basis of some conditions

    {price > 30000 ? <p>"Discount : 10%"</p> : null}
    {price > 30000 && <p>"Discount of 10%"</p>}

Dynamic Component Styling:
    import "./Product.css";
    function Product({ title, price, features }) {
    letisDiscount = price > 30000;
    let styles = { backgroundColor: isDiscount ? "red" : "" };

    return (
        <div className="Product" style={styles}>
        <h3> {title}</h3>
        <h5>Price:{price}</h5>
        {isDiscount && <p>"Discount of 10%"</p>}
        </div>
    );
    }
    export default Product;


Activity (MsgBox.jsx)

Install React Developer Tools:
    https://react.dev/link/react-devtools

                                    Part-3

Events in React:

    1.Handling Click Events

    function dosomethingButton() {
    console.log("Button was clicked !");
    }

    export default function Button() {
    return (
        <div>
        <button onClick={dosomethingButton}> Click me</button>
        <p onClick={dosomethingPara}>This a random paragraph!</p>
        </div>
    );
    }

    2.Handling Non-Click Events

        function handleMouseOver() {
        console.log("You are inside paragraph!");
        }

        <button onDoubleClick={handleDblClick}>Dbl click me!</button>

    Event Object:
        We can access the Event Object in Handler
        Eg:Form.ejx

State In React:
    The state is built-in React Object that is used to contain data or information about the component.
    A component's state can change over time; whenever it changes, the component re-renders.

    component --> function --> render
    component --> state // state --> component then re-render

Hooks:
    Hooks are a new additional in React 16.8
    They let you use state and other react features without writing a class.

    useState:
        useState is a React Hook that lets you add a state variable to your component.
        const [state, setState] = useState(initialState)

    Returns 
        useState returns an array with exactly two values:

        The current state. During the first render, it will match the initialState you have passed.
        The set function that lets you update the state to a different value and trigger a re-render.

Create LikeButton:
    useState is a React Hook that let's you add a state variable to your component.

Closure:
    A closure is a feature in JavaScript where an inner function has access to other (enclosing) function's variables
        * In JS,once a function completes it's execution, any variable that were defined inside the function scope cease to exist.
    eg:
        function outer(){
            let a =10;
            function inner(){
                let b = 20;
                console.log(a+b);
            }
            return inner;
        }

Re-render : How it works ?
    import { useState } from "react";
    export default function Counter() {
    let [count, setCount] = useState(0);
    console.log("Component is Rendered!");
    console.log(count);

    let incCount = () => {
        setCount(count + 1);
        console.log(`Count inside from inCount ${count}`);
    };
    return (
        <div>
        <h3>Count = {count}</h3>
        <button onClick={incCount}>Increase Count</button>
        </div>
    );
    }

Callback in Updater Function:
    i. new state value depend on old state value 
    ii.new state value --> normal flow
    How to change state when it depends on the current value 

    import { useState } from "react";
    export default function Counter() {
    let [count, setCount] = useState(0);

    let incCount = () => {
        setCount(count + 1);
        setCount((currCount) => {
        return currCount + 1;
        });
        setCount((currCount) => {
        return currCount + 1;
        });
        setCount(25);
    };
    return (
        <div>
        <h3>Count = {count}</h3>
        <button onClick={incCount}>Increase Count</button>
        </div>
    );
    }

More about State:


                                    Part-4
Objects and State:
    Ludo Board

Array and State:
    
Create Todo:
    newTodos ->when input change 
    todos ->when btn click

Unique Key For List Items: uuid

Deleting From Array: use filter method

Updating in Arraya:

    Updating All element in Array
        let upperCaseAll = () => {
        setTodos((prevTasks) =>
            prevTasks.map((todo) => {
                return { ...todo, task: todo.task.toUpperCase() };
            })
            );
        };

    Updating One element in Array:

         let upperCaseOne = (id) => {
        setTodos((todos) =>
        todos.map((todo) => {
        if (todo.id === id) {
          return { ...todo, task: todo.task.toUpperCase() };
        } else {
          return todo;
            }
        })
        );
    };

Changing Todo:
    Implement "Done Task" feature to the todo


                                    Part-5

Lottery:
    We are given Lottery ticket with a three digit number
    The number is generated randomly
    If the sum of all three digits is 15. We win the Lottery and get the price

Change to Lottery Game:
    Make a Lottery ticket of N digit number
    The winning Sum could be any feasible number

    Lifting up state:

        App --> Lottery(game)  --> ticket --> ticket --> num 1, TN2,TNn

    Components:
            -->Props
            -->State
            -->Event
Types of React component:
    1.Functional (logical) Components:
        -->smart
        -->State
        -->change(logic)

  2.Class (Presentainal) Components:
        -->dump
        -->UI
        -->no state

Task:
   Path: App --> Lottery --> Ticket --> TicketNum

Lottery Component:

    TicketNum:
        props:num
        State:none
        Event:none

    Ticket:
        props:ticket[] of size n
        State:none
        Event:none

    Lottery
        props:n,winningSum
        State:ticket[]
        Event:buyTicket()

Function as Props:
    JS Function are 1st Class Object

    This means they can  be passed to a function as argument,returned from it & assined to a variable

                                        Part-6

Forms in React:
    The standard way with the Forms is to use Controlled Components.So we make React State to be the "single source of truth"

    In HTML, form elements such as <input>, <textarea>, and <select> typically maintain their own state and update it based on user input. In React, mutable state is typically kept in the state property of components, and only updated with setState().

    We can combine the two by making the React state be the "single source of truth". Then the React component that renders a form also controls what happens in that form on subsequent user input. An input form element whose value is controlled by React in this way is called a "controlled component".

 eg:input change --> onChange -->handleNameChange --> setFullName (rev)

 Form Level In React:
    label ->for --> htmlFor
    input --> id 

Handling Multiple Input:
    We create a common handleInputChange() for all elements

    event.target.name  --> changed field
    event.target.value --> new value of the field

Comments form:
Show Commets:
    commentForm --> submit --> Comments

Validation:
    Formik
        npm install formik   

useEffect():
    The Effect Hook lets you perform side effects in function Components

    Data fetching , setting up a subcription , and manually changing the DOM in React Components are all examples of side effect.

useEffect:
    useEffect is a React Hook that lets you synchronize a component with an external system.

    useEffect(func)
    useEffect(setup, dependencies?(optional))

        dependencies-->stateVariable
        1.[s1,s2] -> it work on this state
        2.[] -> 1st render only not re-render

Use Cases:
    API Calls -> Asynchronous Operations

