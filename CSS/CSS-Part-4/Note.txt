CSS - Next Step:

Alpha Channel:
    Sets the opacity for a color.
->color visibility  
    Ranges from 0 to 1
    0->hidden
    1->visible
syntax:
    rgba(255,255,255,0.3)
    #(ff00ffa1)

Opacity:
    Sets the opacity for the elment
->elment visibility
->Ranges from 0 to 1
     0->hidden
     1->visible
syntax:
        opacity:0.5


CSS Transitions:
    Transitions enable you to define the transition between two states of an element.

Transitions Shorthand:

            property name | duration | timing-function | delay
transition: margin-top          2s     ease-in-out       0.2s

CSS TRANSFORM:
This property let you rotate,scale,skew, or translate an element.

    i.rotate:
    ->turn according to degree.
        trasform: rotate(45deg);

    ii.scale:
    ->the size is increase or decrease by x.
        trasform:scale(x); //

    iii.translate:
    ->Move along x-axis and y-axis
     transform:translate(50px,50px);

    iv.skew:
morna
    transform:skew(30deg);

BOX SHADOW:
    it use to give shadow to the box.
    
    syntax:
    box-shadow:x of set | y of set | blur |color of shadow
    box-shadow:2px 2px 5px black;


BACKGROUND IMAGE:
Let you set an image as a background.

    background-image:url("..some path| image.jpg ")

    background-size:contain | cover | auto;

    contain-->didn't crop and scale ,repeat the image
    cover-->it crop the image
    auto-->streth the image


POSITION:
->The position css property sets how an element is positioned in a document.
->The top , right , bottom and left properties determine the final location of positioned elements.

 i.static:
 The top ,right,bottom,left, and z-index properties have no effect.
 This is the default value.
    position:static;

 ii.relative:
 The offset is relative to itself based on the values of top,right,bottom and left.
    position:relative;

 iii.absolute:
 >with respect to parent 
 The element is removed from the normal document flow, and no space is created for the element in the page layout.
 It is positioned relative to its closest positioned ancestor, if any;otherwise,it is placed relative to the initial containing block.
    position:absolute;

 iv.fixed:
 The element is removed from the normal document flow, and no space is created for the element in the page layout.
 It is positioned relative to the initial containing block established bt the viewport.
    position:fixed;

 Task explore (sticky)
